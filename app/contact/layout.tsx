import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact Us | Order Inquiry | Mitun Cards",
  description: "Get in touch with us for custom invitation card inquiries and orders.",
}

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
